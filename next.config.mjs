/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "max-themes.net",
      "example.com",
      "firebasestorage.googleapis.com",
    ],
  },
};

export default nextConfig;
